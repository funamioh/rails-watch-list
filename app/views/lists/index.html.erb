<div class="container">
<%= cl_image_tag("yvtwx4x5cdlppwhqxfwi", crop: :fill) %>
  <h1>Lists</h1>

<div class="d-flex">
  <% @lists.each do |list| %>
  <div class="card col-4">
    <%= render list %>
    <p>
      <%= link_to 'Show this list', list_path(list) %>
    </p>
</div>
    <% end %>
    </div>
    <%= link_to 'New list', new_list_path %>
</div>
